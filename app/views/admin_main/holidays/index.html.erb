</aside><!-- End Sidebar-->
  <main id="main" class="main">

    <div class="pagetitle">
      <h1>Holidays</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item">Home</li>
          <li class="breadcrumb-item">Holiday</li>
          <li class="breadcrumb-item active">List</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->

    <section class="section">
      <div class="row">
        
        <div class="col-lg-12">

          <div class="card">
            <div class="p-3">
                <%= link_to 'Add Holiday', new_admin_main_holiday_path, class: 'btn btn-primary'%>
            </div>

            <div class="card-body">
             

              <!-- Table with stripped rows -->
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Holiday Name</th>
                    <th scope="col">Holiday Type</th>
                    <th scope="col">Date</th>
                  </tr>
                </thead>
                <tbody>
                  <% @holidays.each do |holiday| %>
                  <tr>
                    <th scope="row"><%= holiday.id %></th>
                    <td><%= holiday.holiday_name&.capitalize() %></td>
                     <td><%= holiday.holiday_type&.capitalize() %></td>
                    <td><%= holiday.holiday_date %></td>
                    <td>
                      <%= link_to 'Edit Holiday', edit_admin_main_holiday_path(holiday), class: 'btn btn-primary'%>
                    </td>
                    <td><%= link_to 'Destroy',  admin_main_holiday_path(holiday),method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary"%>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
              <!-- End Table with stripped rows -->

            </div>
          </div>

        </div>
      </div>
    </section>

  </main><!-- End #main -->